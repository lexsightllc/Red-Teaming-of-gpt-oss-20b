{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "ModerationDecisionCard",
  "type": "object",
  "required": [
    "content_id",
    "user_id",
    "cohort",
    "policy_rule",
    "decision",
    "confidence",
    "signals",
    "alternatives",
    "appealable"
  ],
  "properties": {
    "content_id": { "type": "string" },
    "user_id": { "type": "string" },
    "cohort": { "type": "string" },
    "policy_rule": { "type": "string" },
    "decision": { "type": "string", "enum": ["remove", "allow", "escalate"] },
    "confidence": { "type": "number", "minimum": 0.0, "maximum": 1.0 },
    "signals": {
      "type": "object",
      "properties": {
        "toxicity": { "type": "number" },
        "harassment": { "type": "number" },
        "nsfw": { "type": "number" },
        "context_score": { "type": "number" }
      }
    },
    "alternatives": {
      "type": "array",
      "minItems": 1,
      "items": {
        "type": "object",
        "required": ["label", "threshold_delta", "expected_effect"],
        "properties": {
          "label": { "type": "string" },
          "threshold_delta": { "type": "number" },
          "expected_effect": { "type": "string" }
        }
      }
    },
    "appealable": { "type": "boolean" },
    "notes": { "type": "string" }
  }
}
