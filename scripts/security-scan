#!/usr/bin/env bash
# SPDX-License-Identifier: MPL-2.0

set -euo pipefail
source "$(dirname "$0")/_common.sh"

bandit -q -r src
pip-audit -r requirements/base.txt -r requirements/dev.txt "$@"
"${REPO_ROOT}/scripts/license-scan"
pre-commit run detect-private-key --all-files
